# Copy to .env.local — base URL backend (không có /api ở cuối)
NEXT_PUBLIC_API_BASE_URL=http://localhost:8080

# Google Identity Services — cùng OAuth Client với backend (google.client.id)
NEXT_PUBLIC_GOOGLE_CLIENT_ID=

# Facebook SDK — App ID khớp cấu hình BE
NEXT_PUBLIC_FACEBOOK_APP_ID=
